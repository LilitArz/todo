import { List } from './List';
import { ToDoFilter } from './ToDoFilter';

export const ToDoList = () => {
    return (
        <div>
            <List />
            <ToDoFilter/>
        </div>
    );
};
